require 'order'

describe Order do
  order = Order.new

  it 'can calculate the total for a line item' do
    # xyz = { dish: "Fish", price: 4, qty: 1 }
    # order.add "Fish"
    # expect(order.view).to eq xyz
  end

  it 'can calculate the total for multiple line item' do
  end
end
